"""Config for the Monitor app."""
from django.apps import AppConfig


class MonitorConfig(AppConfig):
    """Config for the Monitor app."""

    name = "monitor"
    verbose_name = "Monitor"
    create_group = False
