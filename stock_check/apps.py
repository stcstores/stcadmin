"""Config for Stock Check app."""

from django.apps import AppConfig


class StockCheckConfig(AppConfig):
    """Config for Stock Check app."""

    name = "stock_check"
    verbose_name = "Stock Check"
    create_group = True
