"""Config for Print Audit app."""

from django.apps import AppConfig


class PrintAuditConfig(AppConfig):
    """Config for Print Audit app."""

    name = "print_audit"
    verbose_name = "Print Audit"
    create_group = True
