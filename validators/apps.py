"""Config for the Validators app."""

from django.apps import AppConfig


class ValidatorsConfig(AppConfig):
    """Config for the Validators app."""

    name = "validators"
    verbose_name = "Validators"
    create_group = True
