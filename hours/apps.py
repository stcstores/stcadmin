"""Config for Hours app."""

from django.apps import AppConfig


class HoursConfig(AppConfig):
    """Config for Hours app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "hours"
