"""Config for shipping app."""

from django.apps import AppConfig


class ShippingConfig(AppConfig):
    """Config for shipping app."""

    name = "shipping"
    verbose_namme = "Shipping"
    create_group = True
